<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Phase 186 / Phase 196: BLE permissions for react-native-ble-plx
         (OBD-II adapter connection). Runtime request of BLUETOOTH_SCAN +
         BLUETOOTH_CONNECT on Android 12+ is handled by
         src/screens/HomeScreen.tsx.

         Phase 196: BLUETOOTH_SCAN carries usesPermissionFlags="neverForLocation"
         — MotoDiag's BLE scan exists solely to find an OBD-II adapter, never to
         derive the user's location, so the app declares it does not use scan
         results for location. This is the F40 cross-platform-parity partner of
         the iOS NSBluetoothAlwaysUsageDescription key (ios/MotoDiag/Info.plist).
         ACCESS_FINE_LOCATION is retained for Android 11 and older, where a BLE
         scan still legally requires it (the neverForLocation flag is honored on
         API 31+ only). -->
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"
        android:maxSdkVersion="30" />
    <uses-feature android:name="android.hardware.bluetooth_le" android:required="true" />

    <!-- Phase 191: Camera + microphone for video diagnostic capture
         (react-native-vision-camera).
         Permission flow: src/hooks/useCameraPermissions.ts wraps
         Camera.requestCameraPermission() + requestMicrophonePermission()
         from vision-camera. Files stay in app-private DocumentDirectoryPath
         so READ_MEDIA_* perms are NOT required for Phase 191
         (Phase 192 share-sheet integration will need them). -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.microphone" android:required="false" />

    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="false"
      android:theme="@style/AppTheme"
      android:usesCleartextTraffic="${usesCleartextTraffic}"
      android:supportsRtl="true">
      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
      </activity>
    </application>
</manifest>
